{
  "scripts": {
    "start": "node server.js --watch",
    "compose:up": "docker-compose -f mongo.yaml up",
    
    "compose:down": "docker-compose -f mongo.yaml down"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^5.3.0"
  }
}
